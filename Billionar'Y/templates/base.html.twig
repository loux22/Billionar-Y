<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>Billionar'Y - {% block title %}{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
    {% for message in app.flashes('success') %}
			<div class="alert alert-success">{{ message }}</div>
		{% endfor %}

		{% for message in app.flashes('errors') %}
			<div class="alert alert-danger">{{ message}}</div></body> 
		{% endfor %} {# end message erreur et success #}
        {% block body %}{% endblock %}
        {% block javascripts %}{% endblock %}
    </body>
</html>
